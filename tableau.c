#include<stdio.h>

int main()
{
    // Déclaration d'un tableau
    float notes[4]={5.5,50.2,30.6,80.9};

// affichage des éléments du tableau
    printf("Les elements du tableau sont :\n");

    printf("En position[0] = %.1f \n", notes[0]);
    printf("En position[1] = %.1f \n", notes[1]);
    printf("En position[2] = %.1f \n", notes[2]);
    printf("En position[3] = %.1f \n", notes[3]);
}